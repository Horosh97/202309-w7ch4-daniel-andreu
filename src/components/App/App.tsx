import SlothForm from "../SlothForm/SlothForm";

export const App = (): React.ReactElement => {
  return (
    <div className="app">
      <SlothForm></SlothForm>
    </div>
  );
};

export default App;
